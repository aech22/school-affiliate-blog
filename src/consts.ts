// サイト共通の設定。外部サービスの「あなたのコード」をここに貼るだけで有効化される。
// いずれも空文字なら無効（タグを出力しない＝計測もしない）。

// Google Analytics 4 の測定ID（例: 'G-XXXXXXXXXX'）。ガジェナビ用に新規発行したIDを貼る（picknaviのIDを使わないこと）。
export const GA_MEASUREMENT_ID = '';

// Pinterest ドメイン認証コード（Pinterestの「ドメインを申請」で表示される <meta> の content 値）。ガジェナビのドメインで新規取得したら貼る。
export const PINTEREST_VERIFY = '';

// X(Twitter) の公式アカウント（例: '@ガジェナビ_jp'）。カード下部に表示される。
export const TWITTER_SITE = '';
